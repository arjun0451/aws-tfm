# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added
- `terraform/` project: VPC, subnets, IGW, NAT, route tables, ALB, EC2, security groups, IAM, optional SSM endpoints.
- `user-data/server1.sh` → `"Hello from server1"`
- `user-data/server2.sh` → `"Hello from server2"`
- `README.md`, `ARCHITECTURE.md`, `CHANGELOG.md`.

### Ops
- Pinned `aws` provider `~> 6.0`; Terraform `>= 1.5`.

### Not done
- Terraform `apply` has not been run.
